package edu.hm.cs.projektstudium.findlunch.androidapp.rest;


/**
 * The enum Request reason
 * contains reasons for
 * a REST request.
 */
public enum RequestReason {
    /**
     * Search request reason.
     */
    SEARCH, /**
     * Refresh request reason.
     */
    REFRESH
}
